#pragma once
#include <iostream>
#include <vector>
# include "Graph.h"
# include "Node.h"
#include <queue>

class BFS
{
public:
	//std::vector<Node> BFS(Graph graph, Node root, Node goal);
	~BFS();
private:
	
};

